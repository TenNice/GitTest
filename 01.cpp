#include <iostream>
using namespace std;

/*
int main() //main이란 함수를 만든 것
{
	////std::cout << "Hello World" << std::endl; //cout, endl은 iostream이라는 라이브러리에 있는 것. std라는 이름공간 안에 있는 cout, endl사용.
	//cout << "Hello" <<  " World" << endl;

	//int a = 100.2; //알아서 100으로(반올림). 32-bit 안에 들어오는 정수. 대략 -10억~10억.
	//float b = 0.2;
	//char c = 'X';
	//bool d = true; //true는 1, false는 0
	//long e = 100; //long도 정수타입. 64-bit 안에 들어오는 정수. long이라고 무조건 64-bit은 아니다. 운영체제 bit 수에 따라 달라진다.
	//double f = 0.9345938245789347532945873245;
	//const int f = 1000; //상수. 변하지 않는 값. const는 초기화 안 하고 선언만 하면 에러난다. 얘도 메모리에 32bit공간을 차지하고 있는거다.

	//cout << a << " " << b << " " << c << " " << d << endl;
	//a = 200;
	//cout << a << endl;
	//a = 300;
	//cout << a << endl;

	//cout << "1 + 1 = " << 1 + 1 << endl;
	int a = 1;
	float b = 1;

	cout << "값을 입력해주세요." << endl;
	cin >> a; //int a인데 asdfasdf입력하니까 0으로 되네. 이상한 값 넣으면 알아서 0으로 되는듯.
	cout << "다음 값을 입력해주세요." << endl;
	cin >> b;

	cout << "입력값1 : " << a << endl;
	cout << "입력값2 : " << b << endl;
	cout << "입력값1 + 입력값2 = " << a + b << endl;
	cout << "입력값1 - 입력값2 = " << a - b << endl;
	cout << "입력값1 * 입력값2 = " << a * b << endl;
	cout << "입력값1 / 입력값2 = " << a / b << endl; //이거는 b에 음수 넣으니까 0되네. 소수부분 버림이네.
	//cout << "입력값1 % 입력값2 = " << a % b << endl; //이거는 mod. 나머지를 계산해준다. b를 int가 아닌 float으로 바꾸니 안 된다.

	return 0;
}
*/